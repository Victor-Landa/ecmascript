/**
 * Dentro de este archivo vamos a poder tener lógica 
 * separada de nuestro hilo principal.
 */
const hello = () => {
  return 'hello';
}

// Exportamos este módulo que podremos llamar donde lo necesitemos.
export default hello;